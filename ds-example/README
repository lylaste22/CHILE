Example of python + openmp (shared memory paralelism) + slurm (bash) scripting + coding

how to use it?

module load astro
cd test_omp
make
cd ..
sbatch myscript.slurm
squeue

check the results files

enjoy

JcM
